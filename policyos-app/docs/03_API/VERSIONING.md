# API Versioning

- Major compatibility boundaries use route versions such as `/api/v1`.
- Additive fields may be introduced within a version.
- Removing or changing field meaning requires a new major API version or a migration window.
- Deprecations must include replacement guidance and a removal date.
