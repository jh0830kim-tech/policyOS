# REST Rules

- GET must not change server state.
- POST creates resources or initiates commands.
- PATCH updates selected fields.
- DELETE removes or archives according to policy.
- Use pagination for list endpoints.
- Use stable machine-readable error codes.
- Request and response schemas must be explicit.
- Sensitive fields are excluded by default.
