# Codex Prompt — Sprint 2 Step 9: Git Commit and Push

Read `CODEX.md` and inspect the complete Sprint 2 diff.

## Preconditions

Do not continue unless:

- `ruff check .` passes
- `pytest` passes
- `git diff --check` passes
- no secrets are present
- the user has explicitly approved committing and pushing

## Required work

1. Show `git status`.
2. Summarize the final diff.
3. Confirm the current branch is `feature/sprint-2-auth-identity`.
4. Stage only Sprint 2 and associated documentation files.
5. Create a clear commit.

Recommended commit message:

```text
feat: complete Sprint 2 authentication and RBAC
```

6. Show the commit result.
7. Push the current branch to its matching remote branch.
8. Do not force-push.
9. Do not merge.

## Completion report

Report:

- branch
- commit hash
- commit message
- push result
- remote branch
- any excluded files
